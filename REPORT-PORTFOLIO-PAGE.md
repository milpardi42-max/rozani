# گزارش کار — صفحه‌ی پورتفولیو و «معرفی کامل راضیه خیری‌پور»

**شاخه:** `arena/01a0cf92-rozvelt` · **PR:** [#1](https://github.com/milpardi42-max/Rozvelt/pull/1)

خواسته‌ی شما: «ساختار صفحه‌ی پورتفولیو را با دقت بررسی کن؛ می‌خواهم صفحه‌ی اصلی پورتفولیو یک ساختار
دیگر داشته باشد؛ یک قسمت معرفی کامل و اختصاصی داشته باشد؛ معرفی راضیه خیری‌پور را بدهد و صفحه کامل
باشد.» — طبق پاسخ‌های خودتان: صفحه‌ی اصلی = `/portfolio`، هر دو صفحه یکی شوند، معرفی کامل در ابتدای
صفحه و بعد گرید پروژه‌ها، و متن معرفی را من بنویسم.

---

## ۱) بررسی دقیق ساختار (پیش از تغییر)

| صفحه | ساختار پیشین |
|---|---|
| `/{locale}/portfolio` — «پورتفولیو» در منوی اصلی | بنر (بردکرامب + «گالری پروژه‌های اجراشده» + «از دیوار تا فضا…») ← نوار آمار (۶ پروژه · ۶ دسته · ۳ طراح) ← فیلترها ← گرید ۶ پروژه‌ی اجراشده ← هر کارت به `/{locale}/portfolio/{slug}` می‌رود. **هیچ معرفی‌ای از بنیان‌گذار نداشت.** |
| `/{locale}/razieh` — پورتفولیوی شخصی | کامل ولی **یتیم**: هیرو تمام‌صفحه + منوی داخلی (درباره/نمونه‌کارها/آکادمیک/تماس)، «از الگو تا فضا» با سه پاراگراف و آمار، ۸ نمونه‌کار با فیلتر و مودال، نقل‌قول، بخش آکادمیک با ۴ دستاورد و ۴ درس، فرم تماس. از هیچ‌جای سایت لینک نشده بود (نه منو، نه فوتر، نه سایت‌مپ). |
| `/{locale}/artists/razieh-khairipour` | پروفایل هنرمند در بخش «هنرمندان»: تقریباً خالی (۰ الگو · ۰ محصول · ۰ پروژه · ۰ دنبال‌کننده) با بیوی یک‌خطی «مدرس و میزبان آکادمی». |
| `/{locale}/portfolio/{slug}` | صفحه‌ی جزئیات هر پروژه (گالری، اسکرول‌پروگرس، اشتراک‌گذاری). |
| `/{locale}/artist/portfolio` · `/{locale}/owner/portfolio` | پنل‌های مدیریتی داخلی (مدیریت کارهای هنرمند · مدیریت محتوای پروژه‌ها) — دست‌نخورده ماندند. |

دو نکته‌ی مهم که در این بررسی پیدا شد: عکس `hero-back.webp` که در «درباره»ی صفحه‌ی شخصی با alt
«پرتره‌ی راضیه خیری‌پور» استفاده شده، در واقع **یک اتاق خواب با کاغذدیواری** است (نه پرتره)؛ و سال
تأسیس در آن صفحه «۱۳۸۸» نوشته شده بود در حالی که صفحه‌ی «درباره ما» تأسیس آتلیه را «۱۴۰۲/۲۰۲۳»
می‌گوید — در متن تازه این دو از هم جدا شده‌اند: ۱۳۸۸ = **آغاز کار حرفه‌ای**، ۱۴۰۲ = **تأسیس رزی آتلیه**.

## ۲) ساختار تازه‌ی صفحه‌ی اصلی پورتفولیو

`/{locale}/portfolio` حالا این‌طور خوانده می‌شود:

1. **بنر صفحه** — همان بردکرامب و عنوان قبلی.
2. **معرفی کامل و اختصاصی** — تصویر کارگاه/فرآیند با کادر تزئینی و نشان «از سال ۱۳۸۸» · عنوان
   «معرفی بنیان‌گذار» · نام «راضیه خیری‌پور» + نام لاتین · سمت («طراح الگو، کاغذدیواری، پارچه و پرده ·
   استادیار هنرهای تزئینی») · **بیوگرافی کامل در پنج پاراگراف** · چیپ‌های تخصص · **امضای پایانی** ·
   دو دکمه («آثار و پروژه‌های اجراشده» ← همین صفحه، «پورتفولیوی شخصی راضیه» ← `/{locale}/razieh`).
3. **چهار عدد کلیدی** — سال تجربه · الگو و طرح خلق‌شده · نمایشگاه داخلی و بین‌المللی · دانشجو و همراه آکادمی.
4. **مسیر حرفه‌ای + در یک نگاه** — چهار نقطه‌عطف (۱۳۸۸ · تدریس و پژوهش · ۱۴۰۲ تأسیس آتلیه · امروز) در
   کنار چهار دانستنی (جایگاه دانشگاهی، حوزه‌ی کار، مستقر در، زبان‌ها) و کارتِ ورود به صفحه‌ی شخصی.
5. **آثار** — تیتر تازه‌ی «آثار منتخب و پروژه‌های اجراشده»، همان نوار آمار، همان فیلترها و همان گرید ۶
   پروژه (دست‌نخورده).
6. **نوار پایانی همکاری** — «همکاری، سفارش سازمانی یا کارگاه» با چهار راه ورود: تماس، آکادمی، درباره‌ی
   آتلیه، همکاری به‌عنوان طراح.

**«هر دو یکی شوند»:** معرفی `/{locale}/portfolio` به `/{locale}/razieh` لینک می‌دهد (هم دکمه‌ی اصلی، هم
کارت)، صفحه‌ی شخصی در منوی هیروی خودش لینکی به «رزی آتلیه» یعنی `/{locale}/portfolio` گرفت (هم نسخه‌ی
دسکتاپ هم موبایل)، و این صفحه‌ی یتیم از این پس از **فوتر** و **sitemap.xml** هم لینک می‌شود.

## ۳) جای متن‌ها

تمام متن معرفی به‌صورت داده‌ی دوزبانه در `src/lib/razieh-profile.ts` است (پاراگراف‌ها، اعداد، مسیر،
دانستنی‌ها، پیام همکاری) و کامپوننت `src/components/portfolio/PortfolioIntro.tsx` آن را **سمت سرور**
رندر می‌کند؛ یعنی همه‌ی خطوط معرفی در همان پاسخ HTML اول حاضرند. برای ویرایش متن‌ها فقط همین یک فایل
کافی است.

## ۴) تأیید زنده (روی بیلد پروداکشن)

- ریگ تازه‌ی `scripts/marketplace-smoke/portfolio-e2e.sh`: **۷۸ بررسی، همه سبز، بدون skip و بدون
  هشدار** (`/tmp/rig-portfolio3.log`) — برای هر دو زبان: کامل بودن معرفی (عنوان، نام، سمت، پاراگراف
  آخر بیوگرافی، چهار عدد، چهار نقطه‌عطف، دانستنی‌ها)، **جای معرفی پیش از تیتر آثار و پیش از مرز گرید**،
  سالم بودن گالری (۶ کارت + نوار آمار + نوار پایانی)، لینک دوطرفه‌ی دو صفحه، و وجود لینک فوتر و
  ورودی‌های sitemap.
- `pages.sh` با چهار خط تازه (`/fa|en/portfolio`، `/fa|en/razieh`) سبز؛ `artist-dashboard-e2e.sh` هم
  دوباره **۸۲ ✔ / ۰ ✘ / ۰ ↷** گرفت (بدون رگرسیون)؛ `tsc --noEmit` و `eslint` پاک.
- بررسی زنده: `/fa|en/portfolio` و `/fa|en/razieh` هر چهار تا ۲۰۰؛ در HTML فارسی، ترتیب دقیقاً
  «معرفی بنیان‌گذار → بیوگرافی → اعداد → مسیر حرفه‌ای → در یک نگاه → تیتر آثار → نوار آمار → گرید →
  همکاری» است؛ در نسخه‌ی انگلیسی هم همه‌ی معادل‌ها حاضرند.
- تصویر کارگاه (`/images/portfolios/pf-process.jpg`) با `next/image` و `priority` سرو می‌شود.

## ۵) نکته‌ی محتوایی برای شما

اعداد و جایگاه‌ها (۱۵+ سال · ۲۰۰+ الگو · ۱۲ نمایشگاه · ۱٫۲k دانشجو · استادیار گروه هنرهای تزئینی ·
مستقر در تهران) را از محتوای خودِ سایت برداشتم تا صفحه با بقیه‌ی سایت هم‌خوان باشد. اگر عدد یا
عنوانی را می‌خواهید عوض کنید، فقط `src/lib/razieh-profile.ts` را بگویید — یا بگویید تا من عوض کنم.
همچنین اگر عکس پرتره‌ی واقعی خودتان را دارید، جای تصویر کارگاه می‌گذارم.
