# آکادمی — صفحه‌ی حرفه‌ای با محتوای ۱۰۰٪ واقعی

این سند کاری است که روی صفحه‌ی آکادمی انجام شد: ناوبری جدید، ویدیوی واقعی هیرو، حذف داده‌های
ساختگی و مسیر ثبت‌نام واقعی. هیچ بخشی از محتوای قبلی صفحه حذف نشد؛ هرچه بود سر جایش مانده (برای
SEO) و همه‌ی عددها حالا از داده‌ی واقعی محاسبه می‌شوند.

---

## ۱. ناوبری (فقط دکمه‌ها)

ترتیب دکمه‌های اصلی هدر دقیقاً به این شکل شد — بدون تغییر در محتوای هیچ‌کدام از صفحه‌ها:

| # | دکمه | مسیر |
|---|------|------|
| ۱ | آکادمی | `/{locale}/academy` |
| ۲ | هنرمندان | `/{locale}/artists` |
| ۳ | پورتفولیو | `/{locale}/portfolio` |
| ۴ | فروشگاه | `/{locale}/shop` (به‌همراه پنل فروشگاه) |

«الگوها» و «فایل دیجیتال» از نوار بالا برداشته نشدند؛ فقط چون نوار بالا قرار بود چهار دکمه‌ی
خواسته‌شده باشد، دسترسی‌شان یک کلیک عمیق‌تر شد و از سه جای دیگر باز است:

- داخل پنل «فروشگاه» (بلافاصله زیر همان دکمه — تراشه‌های الگوها، سبک‌ها، کالکشن‌ها، فایل دیجیتال، اشتراک دانلود)
- منوی موبایل (فهرست زیر دکمه‌های اصلی)
- فوتر سایت (ستون «کشف»)

فایل‌های مرتبط: `src/components/layout/Header.tsx`، `StoreDropdown.tsx`، `nav-data.ts`.

---

## ۲. هیرو: ویدیوی واقعی

ساختار هیرو دست‌نخورده مانده (همان چیدمان، همان متن‌ها، همان کارت دوره‌ی منتخب)؛ فقط جای
تصویر پیش‌نمایش، حالا یک ویدیوی واقعی پخش می‌شود.

- پیش‌نمایش با `<video>` واقعی نمایش داده می‌شود (muted/loop/autoplay، با دکمه‌ی پخش/توقف و بی‌صدا).
- منبع ویدیو اول از خودِ دوره می‌آید: اگر برای آیتم منتخب ویدیو آپلود شده باشد
  (`videoFiles`، اول ویدیوهای رایگان) همان پخش می‌شود.
- اگر چیزی از پنل آپلود نشده باشد، ویدیوی داخلی آکادمی پخش می‌شود:
  `public/videos/academy/preview.mp4` (۱۲۸۰×۷۲۰، ۲۴fps، ۱۶٫۴ ثانیه، ~۳٫۸ مگابایت، H.264، بی‌صدا).

### تولید ویدیوی داخلی

```bash
python3 scripts/academy/make-preview-video.py                  # → public/videos/academy/preview.mp4
python3 scripts/academy/make-preview-video.py --width 1920 --height 1080 --fps 30
python3 scripts/academy/make-preview-video.py --out /tmp/test.mp4 --shot 2.0 --fade 0.5
```

اسکریپت از تصاویر واقعی دوره‌ها (`public/images/education/e01…e06.jpg`) پنج نما می‌سازد، هر نما با
حرکت آرام دوربین، عنوان فارسی همان دوره روی آن، کراس‌فید بین نماها و کارت پایانی «آکادمی رزی».
نیازمندی‌ها: `Pillow`, `arabic-reshaper`, `python-bidi`, `fontTools`, و ffmpeg (از طریق
`imageio-ffmpeg`). متن فارسی همه در Pillow رندر می‌شود، چون بیلد ffmpeg این محیط فیلتر `drawtext`
ندارد.

### جایگزینی ویدیو از پنل ادمین

پنل مدیریت → «آکادمی» → روی هر دوره ویدیو آپلود کنید (`/api/admin/academy/upload-video`، تا ۵۰۰
مگابایت؛ Cloudinary → Vercel Blob → ذخیره‌ی محلی در `public/videos/academy/`). ویدیوی آپلودشده
شده روی همان لحظه در هیرو (اگر آن دوره منتخب باشد) و در بخش «ویدیوهای این دوره» در صفحه‌ی دوره
ظاهر می‌شود. ویدیوهای با `free: false` فقط در پنل و بخش دوره دیده می‌شوند و در گالری عمومی قفل‌اند.

---

## ۳. داده‌های واقعی (حذف عددهای ساختگی)

ماژول جدید `src/lib/data/academy.ts` همه‌ی آمارهای آکادمی را از داده‌ی ذخیره‌شده و واقعی محاسبه
می‌کند — نه هش و نه عدد دستی:

| قبلاً | حالا |
|-------|------|
| قیمت با هشِ شناسه ساخته می‌شد (۲۹/۴۹/۶۹… و دروغین) | قیمت واقعی خود دوره (`item.price`) یا «رایگان» |
| «۲٬۴۰۰+ دانشجو» | تعداد ثبت‌نام‌های واقعی (از رزروها) |
| «۴.۸ ⭐» و تعداد نظرات ساختگی | حذف شد؛ هیچ امتیازی دروغین نمایش داده نمی‌شود |
| تعداد مدرس همیشه ۱ | آمار واقعی هر مدرس (دوره/درس/ویدیو/رویداد) |
| «X از Y صندلی» ساختگی برای رویدادها | ظرفیت واقعی رویداد + تعداد پر شده، و برای دوره‌های خودخوان بی‌نهایت |

آمارهای هیرو (تعداد دوره، مجموع درس‌ها، مجموع ساعت آموزش، رویدادهای زنده، مدرسان) هم از همین
ماژول می‌آیند: `academyOverview(site, reservations)` با `lessonCount` / `lessonMinutes` /
`previewVideoOf` / `formatHours`.

## ۴. ثبت‌نام واقعی

- فرم واقعی در دو جا: کارت هیرو/موبایل مودال آکادمی و سایدبار صفحه‌ی هر دوره/رویداد.
- نقطه‌ی پایانی: `POST /api/academy/enroll` با `{slug, name, email}`.
  - ذخیره در همان مخزنی که پنل «رزرو رویدادها» می‌خواند (`createReservation`).
  - ایمیل تکراری + هم‌ترین اسلاگ = همان ثبت‌نام قبلی (بدون ردیف دوم).
  - ظرفیت رویداد پر باشد → `event_full`؛ رویداد تمام/لغو شده → `event_closed`؛ ورودی نامعتبر →
    `invalid_payload`؛ اسلاگ ناشناس → `not_found`.
  - محدودیت نرخ: ۱۲ درخواست در ۱۵ دقیقه از هر IP (`academy-enroll`).
  - دوره‌ها «خودخوان» ثبت می‌شوند (`eventType: "course"`، بدون ظرفیت) و رویدادها ظرفیت زنده
    دارند.
- بعد از ثبت‌نام پیام واقعی با شناسه‌ی ثبت‌نام و یادآوری هماهنگی پرداخت (اگر قیمت > ۰) نشان داده
  می‌شود؛ برای رویدادها لینک ورود به `/academy/{slug}/live` هم هست.

## ۵. صفحه‌ی دوره

- فرم ثبت‌نام واقعی به سایدبار اضافه شد (قبلاً دکمه‌ی بی‌اثر بود).
- بخش «ویدیوهای این دوره» به‌همراه یک پلیر سبک (`VideoGrid`) که ویدیوهای آپلودشده‌ی همان دوره را
  نمایش می‌دهد.
- تعداد ثبت‌نام/ظرفیت و لیست درس‌ها از داده‌ی واقعی.
- همه‌ی محتوای قبلی (توضیحات، الگوها/محصولات مرتبط، سایدبار پیشرفت) حفظ شده است. نوار «پیشرفت»
  بر اساس درس‌های دیده‌شده‌ی خود کاربر است (در مرورگر ذخیره می‌شود)، پس عددش واقعی است.

## ۶. SEO

- تمام متن‌های قبلی صفحه (توضیحات، دسته‌بندی‌ها، متادیتا) سر جایش است.
- JSON-LD صفحه‌ی آکادمی به `EducationalOrganization` ارتقا یافت و هر دوره `Course` با
  `timeRequired` واقعی و `offers` (فقط وقتی قیمت دارد) را اعلام می‌کند.

---

## ۷. تست

```bash
npm run check                                     # typecheck + lint
bash scripts/marketplace-smoke/pages.sh           # همه‌ی صفحات + صفحه‌های آکادمی + فایل ویدیو
bash scripts/academy/enroll-e2e.sh                # ثبت‌نام واقعی → پنل ادمین → پاکسازی
```

`enroll-e2e.sh` یک ثبت‌نام واقعی می‌سازد، نشان می‌دهد در `/api/admin/reservations` دیده می‌شود،
تکرار از همان ایمیل را بررسی می‌کند، ورودی نامعتبر را می‌سنجد و ردیف آزمایشی را پاک می‌کند.

## ۸. اجرای پروداکشن (نکته‌ی مهم)

`next build` پوشه‌های `public/` و `dist/.next/static/` را داخل `dist/.next/standalone/` کپی
نمی‌کند؛ بدون آن‌ها سرور بالا می‌آید و HTML را جواب می‌دهد، ولی همه‌ی CSS/JS/تصاویر ۴۰۴ می‌شوند
(سایت بدون استایل و بدون تصویر دیده می‌شود). برای همین `postbuild` اضافه شد:

```bash
npm run build        # بعد از build خودش scripts/sync-standalone.mjs را اجرا می‌کند
node scripts/sync-standalone.mjs   # اجرای دستی در هر زمان
```

## ۹. محدودیت‌ها

- ویدیوی داخلی صامت است (قلاب‌های موجود برای صدا از پنل قابل افزودن است).
- پیشرفت درس‌ها در مرورگر کاربر ذخیره می‌شود، نه روی حساب کاربری؛ برای نمایش درصد واقعی در پنل
  کاربری به یک ذخیره‌گاه پیشرفت نیاز است (کار بعدی پیشنهادی).
- پرداخت دوره از مسیر هماهنگی دستی انجام می‌شود؛ اتصال به درگاه فقط برای بازار دیجیتال پیاده‌سازی
  شده است.
