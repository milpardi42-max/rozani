# گزارش کار — بازگشت فرم ثبت‌نام به حالت قبل (با تفکیک محتوا)

**شاخه:** `arena/01a0cf92-rozvelt` · **PR:** [#1](https://github.com/milpardi42-max/Rozvelt/pull/1)

> **به‌روزرسانی پایانی:** بعد از این گزارش، ثبت‌نام دقیقاً به همان شکل اصلی مخزن برگشت — فرم خریدار در
> `/{locale}/signup` و صفحه و فرم هنرمند/طراح در `/{locale}/creators/join`؛ صفحه‌های میانی و کامپوننت‌های
> تازه حذف شدند.

## خواسته‌ی شما

> «در فرم ثبت‌نام، به حالت قبل برگرده؛ فقط می‌خواهم محتوای صفحه‌ی خریدار با محتوای صفحه‌ی هنرمند/طراح
> تفکیک شه. پروفایل هنرمند باید کامل تفکیک باشه از خریدار ولی فرم ثبت‌نام قبلی را باز گردانی کن.»

طبق انتخاب شما در پرسش روشن‌سازی: **صفحه‌ی خریدار دست‌نخورده بماند**، **فرم صفحه‌ی هنرمند/طراح به همان فرم
تک‌صفحه‌ای قبلی برگردد**، و فیلدهای فروشنده (استودیو، سابقه، بیو، فرمت‌ها، دسته‌ها، شرایط) **اختیاری و
بدون گام‌بندی** در همان فرم بمانند.

## چه چیزی عوض شد

### ۱) فرم فروشنده — همان فرم قبلی، تک‌صفحه‌ای

`src/components/profile/CreatorSignupForm.tsx` کاملاً بازنویسی شد و به ساختار قبلی برگشت: یک فرم، یک شبکه،
بدون هیچ گام یا دکمه‌ی «گام بعد/قبل». فیلدهای اجباری دقیقاً همان چهار فیلد حساب‌اند:

| فیلد | وضعیت |
|---|---|
| نام و نام خانوادگی | اجباری |
| ایمیل | اجباری |
| رمز عبور + تکرار رمز | اجباری (حداقل ۶ کاراکتر) |
| شماره تماس | اختیاری |
| نوع فعالیت (حوزه‌ی کار) | اختیاری — منوی کشویی مثل قبل |
| شهر | اختیاری |
| آیدی اینستاگرام | اختیاری |
| لینک پورتفولیو | اختیاری |
| **بلوک استودیو** (نشانی‌شده با برچسب «اختیاری»): نام استودیو، سابقه‌ی کار، درباره‌ی شما، فرمت‌های تحویل (PNG/JPG/AI/PSD/SVG/EPS)، هشت دسته‌ی کاری، تیک پذیرش شرایط | همه اختیاری |

- چیدمان و دکمه‌ی ارسال دقیقاً مثل قبل: «ثبت‌نام به عنوان هنرمند» و پیوند «قبلاً ثبت نام کرده‌اید؟ ورود».
- هیچ‌کدام از فیلدهای اختیاری `required` نیستند و اگر خالی بمانند، **هیچ مقداری ساخته و ذخیره نمی‌شود**.
- فرم همان‌طور که قبلاً سرو می‌شد، در HTML کامل سرو می‌شود (autofill مرورگر و تست‌ها کار می‌کنند).
- کارت موفقیت و کارت «شما قبلاً فروشنده هستید» (پس از ثبت، نه خود فرم) حفظ شد.

### ۲) API ثبت‌نام — اختیاری‌شدن پرونده‌ی فروشنده

`src/app/api/auth/signup/route.ts`: پاک‌سازی مقادیر (طول، کاراکترهای کنترلی، شناسه‌های نامعتبر فرمت/دسته)
سر جایش است، ولی دیگر خطای ۴۰۰ `missing_specialty` و `terms_required` وجود ندارد — چون فیلدها اختیاری‌اند.
`signupTermsAt` فقط وقتی ثبت می‌شود که تیک شرایط خورده باشد (قبلاً بی‌قیدوشرط ثبت می‌شد) و سقف طول «سابقه‌ی
کار» از ۳ به ۱۶ کاراکتر اصلاح شد تا مقدارهای درست (مثل «۸ تا ۱۵ سال» = `8-15`) بریده نشوند.

### ۳) محتوای صفحه‌ی هنرمند و صفحه‌ی خریدار — تفکیک‌شده و حفظ‌شده

- صفحه‌ی فروشنده `/creators/join` همان محتوای کامل و مخصوص فروشنده را دارد: مزیت‌ها، **فرمت‌های تحویل**،
  **هشت دسته‌ی کاری**، لایسنس‌ها، «از ثبت‌نام تا اولین فروش»، «برای شروع چه لازم دارید»، آمار سهم/حداقل تسویه،
  طراحان همراه، فرم ثبت‌نام و پرسش‌های پرتکرار — فقط متن‌هایی که به سه گام اشاره می‌کردند به «همین یک فرم +
  بخش اختیاری» اصلاح شدند.
- صفحه‌ی خریدار `/signup` دست‌نخورده است: همان چهار فیلد، بدون کلید «نوع حساب»، به‌همراه کارت راهنمای
  فروشندگان (متن کارت هم به فرم تک‌صفحه‌ای به‌روز شد).
- راهنمای داخل صفحه‌ی `/artist` برای حساب خریدار (بنر «اینجا خانه‌ی هنرمندان است») هم متنش به فرم تازه
  به‌روز شد.

### ۴) پروفایل هنرمند — هنوز کامل جدا از خریدار

داشبورد `/{locale}/artist` (شاخص‌ها، آثار، تحویل، کیف پول، کارنامه)، استودیوی فروش
`/{locale}/artist/marketplace` و پورتفولیو `/{locale}/artist/portfolio` بدون تغییر باقی ماندند؛ خریدار
همچنان فقط `/account` را دارد و در `/artist` صفحه‌ی راهنما می‌بیند. پنل مدیر هم همان پرونده‌ی هنرمند را
نشان می‌دهد — الان فقط ممکن است چند فیلدش خالی باشد تا هنرمند از داشبورد کاملش کند.

## تأیید زنده (روی بیلد پروداکشن)

ریگ `scripts/marketplace-smoke/artist-dashboard-e2e.sh` بازنویسی و روی سرور تازه اجرا شد: **۷۳ بررسی، همه
سبز، بدون هیچ skip** (خروجی صفر). مهم‌ترین بررسی‌ها:

- بخش ۱: فرم خریدار همان چهار فیلد را دارد، کلید «نوع حساب» نیست، و کارت راهنمای فروشنده سر جایش است (fa/en).
- بخش ۲ (تازه): صفحه‌ی فروشنده همان فرم تک‌صفحه‌ای قبلی را سرو می‌کند (۹ ورودی: نام، ایمیل، تلفن، شهر،
  اینستاگرام، پورتفولیو، رمز، تکرار رمز + بلوک استودیو)، **فقط چهار فیلد حساب `required`** و بقیه اختیاری،
  هیچ گام/دکمه‌ی گامی در HTML نیست، و محتوای فروش (فرمت‌ها، دسته‌ها، سهم، تسویه، بازبینی، پیوند خریدار) هنوز هست.
- بخش ۳: ثبت‌نام واقعی با پرونده‌ی کامل → روی رکورد هنرمند ذخیره می‌شود (`signupStudio` · `signupExperience` ·
  `signupSpecialty` · `signupCity` · `signupFormats` · `signupFamilies` · بیو · شبکه‌های اجتماعی · `pending`)،
  و **ثبت‌نام فقط با فیلدهای حساب هم کار می‌کند** و برای فیلدهای خالی چیزی از خودش نمی‌سازد؛ ایمیل تکراری
  همان‌طور `email_taken` می‌دهد.
- بخش ۴ تا ۸: پنل مدیر همان پرونده را می‌بیند و تأیید می‌کند، خریدار به داشبورد هنرمند نمی‌رسد، داشبورد
  هنرمند اعدادش با API برابر است، پورتفولیو سر جایش است و دکمه‌ی خروج در همه‌ی سطوح هست.

بقیه‌ی ریگ‌ها روی همین بیلد دوباره اجرا و سبز شدند: `http` · `exclusive` · `artist` · `sub` · `family`
(۳۳ بررسی) · `formats` (۶۲ بررسی) · `multipart` · `pages.sh` · ثبت‌نام آکادمی. `npx tsc --noEmit` پاک و
`eslint` صفر خطا.

## چطور ببینید

سرور روی پیش‌نمایش بالاست:

- `/fa/creators/join` → فرم تک‌صفحه‌ای قبلی + بلوک «اطلاعات استودیو — اختیاری».
- `/fa/signup` → همان فرم کوتاه خریدار، با کارت راهنمای فروشندگان.
- با `buyer@example.com` / `buyer-dev-pass` وارد شوید و `/fa/artist` را ببینید (صفحه‌ی راهنما)؛ با
  `niloufar@example.com` / `artist-dev-pass` داشبورد کامل هنرمند را ببینید.
