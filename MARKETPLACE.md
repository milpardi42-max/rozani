# فروشگاه فایل دیجیتال رزی‌آتلیه — گزارش بررسی حرفه‌ای و مستندات اجرا

> این سند دو کار انجام می‌دهد: **(۱)** ارزیابی حرفه‌ای سایت پیش از این کار، و **(۲)** مستندات کامل
> پنج فازی که اضافه شده — چه چیزی واقعی است، چه چیزی شبیه‌سازی‌شده (فقط زرین‌پال)، و دقیقاً چطور
> می‌توان همه را تست کرد.

---

## ۰) خلاصه اجرایی

سایت پیش از این یک وب‌سایت نمونه‌کار/فروشگاه فیزیکی کامل و تمیزی بود، اما **هیچ زیرساختی برای فروش فایل
دیجیتال نداشت**: نه فایل مادر خصوصی، نه لایسنس، نه درگاه پرداخت، نه واترمارک، نه گواهی، نه سهم هنرمند.
همه‌ی این‌ها از صفر و در امتداد همان معماری موجود (App Router، توکن‌های CSS، دیکشنری fa/en، استور
دوگانه) ساخته شد.

نتیجه: یک مسیر خرید دیجیتال **واقعی و سرتاسری** — از آپلود فایل مادر توسط هنرمند تا ورود هنرمند به
فروشگاه، تأیید مدیر، پرداخت، صدور گواهی PDF فارسی، دانلود امضاشده، تسویه سهم هنرمند و پلن اشتراک.

| موضوع | وضعیت |
|---|---|
| درگاه زرین‌پال | **شبیه‌سازی‌شده اما کارا** (بدون `ZARINPAL_MERCHANT_ID` → درگاه آزمایشی داخلی). با گذاشتن مرچنت‌آی‌دی، همان کد به REST واقعی زرین‌پال وصل می‌شود. |
| سایر موارد (آپلود، واترمارک، گواهی، اسکن ویروس، ذخیره‌سازی خصوصی، دانلود امضاشده، سهم هنرمند، اشتراک، کد معرف) | **واقعی** — بدون ماک، روی داده‌ی واقعی و با تست سرتاسری. |

---

## ۱) ارزیابی سایت قبل از این کار (Audit)

**آنچه خوب بود و دست‌نخورده ماند:** معماری App Router با مسیرهای دوزبانه، دیکشنری i18n، استور محتوا
(Upstash → Blob → فایل)، احراز هویت HMAC-کوکی، پنل ادمین محتوا، ریسپانسیو و سیستم دیزاین تمیز.

**شکاف‌ها نسبت به درخواست پنج‌فازی:**

| نیاز | وضعیت قبل |
|---|---|
| آپلود فایل مادر خصوصی | ❌ نبود — آپلود موجود فقط تصویر عمومی سایت را در `public/` می‌گذاشت |
| صف بررسی مدیر برای آثار دیجیتال | ❌ نبود |
| درگاه پرداخت (زرین‌پال) | ❌ نبود (فقط سفارش فیزیکی بدون درگاه) |
| دانلود امن امضاشده | ❌ نبود |
| واترمارک خودکار | ❌ نبود |
| گواهی لایسنس PDF | ❌ نبود |
| فروش انحصاری + حذف خودکار اثر | ❌ نبود |
| موکاپ‌ساز خودکار | ❌ نبود |
| تشخیص بی‌درز (Seamless) | ❌ نبود |
| سیستم رویالتی و تسویه هنرمند | ❌ نبود |
| Stripe + آپلود چندبخشی + ClamAV + ایمیل پیشنهادی | ❌ نبود |
| داشبورد فروش هنرمند | ❌ نبود |
| کد تخفیف/معرف و اشتراک | ❌ نبود |

**دو باگ پنهان که در جریان تست واقعی پیدا و رفع شد:**

1. **هر فروش، اثر را از فروشگاه حذف می‌کرد.** تابع ثبت فروش، اگر اثر *صرفاً یک لایسنس انحصاری داشت*
   (حتی غیرفعال) وضعیت اثر را `sold_exclusive` می‌کرد؛ یعنی با اولین فروش عادی، اثر برای همیشه از
   فروشگاه می‌رفت. اصلاح شد: فقط فروش واقعی لایسنس انحصاری اثر را حذف می‌کند.
2. **شمارش کلیک کد معرف، شمارنده‌ی سریال گواهی را خراب می‌کرد.** شمارنده‌های معرف در همان کلید
   شمارنده‌های سریال لایسنس ذخیره می‌شد و نوع داده‌اش ناسازگار بود (خطای زمان اجرا). یک کلید مستقل
   (`referrals`) ساخته شد.

---

## ۲) فاز ۱ — آپلود خصوصی، بررسی مدیر، درگاه، دانلود امن

**آپلود (`/fa/artist/marketplace` → تب «بارگذاری»)**
- جلسه‌ی آپلود (`POST /api/marketplace/upload/session`) با اعتبارسنجی نوع/حجم/تعداد فایل.
- فایل مادر **همیشه خصوصی** ذخیره می‌شود: `private/masters/<assetId>/original.<ext>` — نه در `public/`
  و نه قابل سرو با کلید خام (تست‌شده: `404` برای مسیر عمومی، `403` برای کلید master در API مدیا).
- فایل‌های بزرگ خودکار **چندبخشی** می‌شوند (`partSize` پیش‌فرض ۸ مگابایت؛ روی ذخیره‌سازی محلی هر فایلِ
  بزرگ‌تر از یک قطعه، روی S3 از ۲۰۰ مگابایت) و قطعه‌ها در `private/staging/<session>/0000N.part` جمع
  می‌شوند؛ بازه‌ی مجاز تا ۲ گیگابایت.
- هر قطعه باید دقیقاً همان اندازه‌ای را داشته باشد که جایگاهش ایجاب می‌کند و با sha256 مرورگر بخواند؛
  فایل کامل هم پیش از ساخت اثر با اندازه‌ی اعلام‌شده و sha256 کل فایل تطبیق داده می‌شود (جزئیات در ۶٫۷).

**بررسی مدیر (`/admin/fa/marketplace`)**
- صف بررسی با پیش‌نمایش واترمارک‌شده، گزارش اسکن ویروس، نتیجه‌ی تشخیص بی‌درز و اطلاعات فایل؛
- اکشن‌ها: تأیید و انتشار، رد (با یادداشت)، اسکن مجدد، بازسازی پیش‌نمایش‌ها، حذف کامل، خارج/داخل کردن از فروشگاه؛
- با هر تصمیم یک ایمیل اطلاع‌رسانی برای هنرمند صف می‌شود.

**درگاه پرداخت**
- زرین‌پال واقعی (`ZARINPAL_MERCHANT_ID` + `ZARINPAL_BASE_URL`): درخواست پرداخت، بازگشت، verify؛
- **بدون مرچنت‌آی‌دی → درگاه آزمایشی داخلی**: صفحه‌ی تأیید واقعی در `/{locale}/checkout/sandbox`
  با مبلغ، پذیرنده، شماره کارت تستی و سه دکمه (پرداخت / ناموفق / انصراف). همان مسیر واقعی پس از آن
  اجرا می‌شود: تأیید امضاشده → سفارش `paid` → صدور لایسنس → گواهی → رویالتی → ایمیل.
- صفحه‌ی آزمایشی **بدون جاوااسکریپت هم کار می‌کند** (فرم واقعی + ریدایرکت ۳۰۳).
- Idempotency: پرداخت دوباره یا بازگشت دوباره از درگاه، لایسنس تکراری صادر نمی‌کند.

**دانلود امن**
- هیچ لایسنسی مسیر فایل را نمی‌بیند. توکن HMAC کوتاه‌عمر (پیش‌فرض ۱۵ دقیقه) با
  `{license, buyer, key, disposition}` امضا می‌شود؛ لینک ایمیل تابع TTL جداگانه (پیش‌فرض ۷ روز) دارد.
- سهمیه‌ی دانلود روی خود لایسنس ثبت می‌شود؛ هر دانلود (زمان، IP، UA) در تاریخچه‌ی لایسنس می‌ماند.
- روی بک‌اند S3 به‌جای استریم، لینک presigned تازه صادر می‌شود.

---

## ۳) فاز ۲ — واترمارک خودکار، گواهی PDF، فروش انحصاری

- **واترمارک خودکار**: هنگام آپلود، سه نسخه ساخته می‌شود — پیش‌نمایش واترمارک‌شده، تامبنیل و کاشی
  ۲×۲ برای نمایش بی‌درز. واترمارک شامل سه خط («Rosie Atelier / PREVIEW / رزی آتلیه») و تگ اختصاصی
  اثر (`assetId`) است. فقط همین فایل‌های مشتق‌شده از `/api/marketplace/media` سرو می‌شوند.
- **گواهی PDF**: A4 دو صفحه‌ای (گواهی + متن کامل قرارداد و خلاصه پرداخت) با QR راستی‌آزمایی.
  متن فارسی به‌صورت **گلیف وکتور شکل‌یافته** (شکل‌دهی بیدی + فونت ایران‌سنس) رندر می‌شود تا در همه‌ی
  مرورگرها یکسان باشد؛ اعداد و شماره‌ها لاتین و قابل جست‌وجو می‌مانند.
- **فروش انحصاری با حذف خودکار**: لایسنس انحصاری به‌صورت پیش‌فرض غیرفعال است تا هنرمند خودش فعال کند؛
  پس از فروش واقعی، اثر `sold_exclusive` + خصوصی می‌شود و از فهرست عمومی می‌رود. در **بازپرداخت**،
  لایسنس باطل و اثر به فروشگاه برمی‌گردد (تست‌شده، بخش ۹).

---

## ۴) فاز ۳ — موکاپ‌ساز، تشخیص بی‌درز، گواهی فارسی، رویالتی و تسویه

- **موکاپ‌ساز خودکار**: پنج موکاپ (قاب، تخت، قاب‌دار، شبکه، پارچه) با ترکیب فایل اصلی و تصویر پس‌زمینه.
- **تشخیص بی‌درز**: اختلاف لبه‌ها با خط پایه‌ی داخل تصویر مقایسه و امتیاز ۰..۱ داده می‌شود
  (`seamless` / `suspected` / `not-seamless`). تست واقعی: الگوی کاشی‌پذیر امتیاز **۰.۹۸۲** گرفت،
  گرادیان غیرکاشی **۰.۰** — در پنل هنرمند و ادمین قابل مشاهده است. گزینه‌ی «تأیید خودکار آثار بی‌درز»
  در تنظیمات ادمین وجود دارد.
- **سیستم رویالتی**: هر فروش در دفتر کل ثبت می‌شود (`sale` / `platform_fee` / `referral` / `reversal`)،
  سهم هنرمند پیش‌فرض ۴۰٪ (قابل تغییر در تنظیمات ادمین یا برای هر اثر)، و توزیع بین هنرمند/پلتفرم/معرف.
- **تسویه هنرمند**: کیف پول با موجودی قابل‌برداشت، حداقل برداشت (پیش‌فرض ۵۰۰٬۰۰۰ تومان / ۲۵ دلار)،
  فرم اطلاعات بانکی (شبا/کارت/PayPal)، درخواست تسویه، و صف تسویه در پنل ادمین با وضعیت‌های
  درخواست‌شده → تأیید → پرداخت‌شده (یا رد). پس از پرداخت، موجودی صفر و `paidOut` پر می‌شود (تست‌شده).

---

## ۵) فاز ۴ — Stripe، آپلود چندبخشی، ClamAV، ایمیل لینک پیشنهادی

- **Stripe (بین‌المللی)**: Charge در ارز دلار (`fa` تومان / `en` دلار)، چک‌اوت Checkout Session اگر
  `STRIPE_SECRET_KEY` باشد؛ در غیر این صورت همان درگاه آزمایشی در حالت USD. webhook/بازگشت و verify
  پیاده‌سازی شده است.
- **آپلود چندبخشی**: مسیر `session → part → complete` با قطعه‌های ۸ مگابایتی (کمینه ۵ مگابایت)، پشتیبانی از
  presigned PUT روی S3 (مرورگر مستقیم به باکت می‌فرستد و فقط etag را ثبت می‌کند) و مونتاژ نهایی
  (`assembleParts`). برای بک‌اند S3، مونتاژ Native (`UploadPart`/`CompleteMultipartUpload`) نیز موجود است.
- **ClamAV**: اگر `CLAMAV_HOST` تنظیم شود، اسکن با clamd انجام می‌شود؛ در غیر این صورت اسکنر هیوریستیک
  داخلی فعال است (EICAR، فایل‌های اجرایی، ماکرو/وب‌شل، بمب فشرده‌سازی). فایل آلوده هرگز به صف بررسی
  نمی‌رسد (تست EICAR در تست خودکار: `infected` → رد).
- **ایمیل**: تحویل سفارش + لینک دانلود امضاشده، رسید اشتراک، اطلاع فروش به هنرمند، اطلاع تأیید/رد به
  هنرمند، اطلاع تسویه، و ایمیل تست. ترتیب ارسال: Resend → SMTP → outbox داخلی (همه‌ی پیام‌ها در پنل
  ادمین قابل مشاهده و ارسال مجدد هستند).

---

## ۶) فاز ۵ — داشبورد فروش، کد معرف/تخفیف، اشتراک

- **داشبورد فروش هنرمند** (`/fa/artist/marketplace`): درآمد، رویالتی، تعداد فروش، بازدید، نرخ تبدیل،
  دانلود، میانگین سفارش + نمودار ۳۰/۹۰/۳۶۵ روزه، به‌تفکیک اثر.
- **کد تخفیف/معرف**: هنرمند کد می‌سازد (درصدی/مبلغی)، لینک معرف
  `/{locale}/marketplace?ref=CODE` **کلیک را می‌شمارد**، در مرورگر ذخیره می‌شود و در پرداخت اعمال
  می‌گردد؛ معرف سهم کمیسیون می‌گیرد. کدهای بی‌اعتبار ثبت نمی‌شوند (تست‌شده).
- **اشتراک**: سه پلن (ماهانه/استودیو/بی‌نهایت) با قیمت تومانی و دلاری، پوشش لایسنس شخصی/تجاری/گسترده،
  سهمیه‌ی دانلود ماهانه، سقف دوره، لغو اشتراک و شمردن مصرف. خرید پلن از همان مسیر پرداخت می‌گذرد و
  دانلود پوشش‌داده‌شده یک سفارش صفر-مبلغ + لایسنس معتبر می‌سازد (تست‌شده).

---

## ۶٫۵) فرمت‌های تحویل و رنگ‌بندی‌ها (تازه)

هر اثر چند **رنگ‌بندی** دارد و هر رنگ‌بندی فایل‌های تحویل خودش را دارد. تعریف فرمت‌ها در
`src/lib/marketplace/formats.ts` تنها منبع حقیقت برای آپلود، ذخیره‌سازی، نمایش و دانلود است:

| فرمت | نقش | حداقل حجم | بررسی محتوا |
|---|---|---|---|
| PNG | تحویل تصویری، شفافیت کامل | ۱ کیلوبایت | امضای `\x89PNG` |
| JPG | تحویل سبک‌تر برای چاپ دیجیتال | ۱ کیلوبایت | `FF D8 FF` |
| تصویر پیش‌نمایش | کاور فروشگاه (اختیاری) | ۱ کیلوبایت | PNG/JPG/WebP |
| AI | فایل منبع ایلاستریتور | ۶۴ بایت | `%PDF` یا `%!PS` |
| PSD | فایل لایه‌دار فتوشاپ | ۶۴ بایت | `8BPS` |
| SVG | وکتور باز وب | ۶۴ بایت | وجود `<svg` |
| EPS | فایل چاپ حرفه‌ای | ۶۴ بایت | `%!PS` |

- **آپلود**: فرم هنرمند برای هر رنگ، یک اسلات برای هر فرمت دارد. یک اثر می‌تواند هر تعداد رنگ داشته
  باشد؛ رنگ اول با فایل PNG یا JPG ساخته می‌شود (پیش‌نمایش، بندانگشتی و کاشی از آن ساخته می‌شود) و
  بقیهٔ فایل‌ها با `attachToAssetId` به همان اثر می‌چسبند. مسیر ذخیره:
  `private/masters/<assetId>/<colourway>/<format>-<stamp>.<ext>`.
- **اعتبارسنجی سه‌لایه**: (۱) پسوند و MIME با فرمت انتخابی باید بخواند، (۲) امضای واقعی فایل بررسی
  می‌شود (`invalid_signature` ۴۲۲)، (۳) اسکن ویروس. برای هر رنگ، پیش‌نمایش واترمارک‌دار جدا ساخته می‌شود.
- **فروشگاه**: صفحهٔ اثر سواچ رنگ‌ها، گالری هر رنگ و جدول «فرمت‌های تحویل» با حجم هر فرمت را نشان
  می‌دهد؛ کارت‌های کاتالوگ فرمت‌ها (با نام خوانا) و نقطه‌های رنگ را دارند.
- **خرید و دانلود**: یک لایسنس همهٔ فایل‌ها را باز می‌کند و سهمیهٔ دانلود هرگز کمتر از تعداد فایل‌های
  تحویل نیست. هر فایل توکن امضاشدهٔ جداگانه دارد؛ توکن معتبری که به فایلی بیرون از همان اثر اشاره کند
  `invalid_token` می‌گیرد. نام فایل تحویلی: `Quiet-Garden-Crimson-PSD.psd`.
- **گواهی، ایمیل و صفحهٔ تحویل**: گواهی PDF ردیف «فرمت‌های تحویل» دارد، ایمیل تحویل فرمت‌ها و تعداد
  رنگ‌ها را می‌نویسد و `/delivery/<token>` فهرست کامل فایل‌ها (رنگ، فرمت، حجم و لینک هرکدام) را
  نشان می‌دهد.
- **بازبینی مدیر**: صف بازبینی رنگ‌ها، فرمت‌ها و حجم کل تحویل را نشان می‌دهد. افزودن رنگ یا فایل تازه به
  اثری که تأیید شده بود، آن را به `pending_review` برمی‌گرداند (و با انتشار فوری، `finalize` پس از رسیدن
  کامل فایل‌ها دوباره منتشرش می‌کند).

## ۶٫۶) ثبت‌نام و داشبورد هنرمند (تازه)

**ثبت‌نام همان دو درِ همیشگی است و هر کدام فرم خودش را دارد:**

- **`/{locale}/signup` — فرم ثبت‌نام:** خودِ فرم انتخاب نوع حساب را دارد («خریدار» / «هنرمند / طراح»؛
  رادیوهای `account_role`) و چهار فیلد حساب (نام، ایمیل، رمز، تکرار رمز)؛ با انتخاب «هنرمند / طراح» حساب
  به‌صورت هنرمند ساخته می‌شود. پوسته‌ی ثبت‌نام بعد از ساختن حساب، کاربر را وارد می‌کند.
- **`/{locale}/creators/join` — هنرمند / فروشنده:** صفحه‌ی ثبت‌نام فروشندگان (مزیت‌ها، طراحان همراه) با فرم
  تک‌صفحه‌ای خودش: نام، ایمیل، تلفن، نوع فعالیت، شهر، اینستاگرام، لینک نمونه‌کار، رمز و تکرارش. فقط چهار فیلد
  حساب `required` هستند و بقیه اختیاری‌اند.
- API ثبت‌نام (`POST /api/auth/signup`) حساب را با همان نوعی که کاربر انتخاب کرده می‌سازد، آنچه داده شده را
  روی رکورد هنرمند می‌نویسد
  (`signupPhone` · `signupCity` · `signupSpecialty` · `signupPortfolioUrl` · هندل اینستاگرام و لینک نمونه‌کار)
  و پرونده را با وضعیت `pending` برای مدیر می‌فرستد.
- پنل مدیر پرونده را نشان می‌دهد و دکمه‌ی تأیید دارد.

- **صفحه‌ی پروفایل هنرمند/طراح:** مسیر `/{locale}/account` برای هنرمند/طراح **فقط محتوای داشبورد هنرمند**
  را نشان می‌دهد — همان پنل سرور-رندرشده‌ی `/{locale}/artist` (`ArtistDashboardPanel`)، بدون هیچ‌کدام از بخش‌های
  حساب خریدار (خلاصه حساب، رزروها، سفارش‌ها، تنظیمات) و بدون بنر پروفایل. دو مسیر عیناً یک محتوا دارند.
  حساب‌های خریدار همان صفحه‌ی حساب قبلی خود را دست‌نخورده دارند.

**داشبورد هنرمند** — `/{locale}/artist` حالا یک داشبورد واقعی و سرور-رندرشده است (داده‌ها از
`src/lib/artist/dashboard.ts`):

- هویت هنرمند: آواتار، نام، تخصص، شهر، استودیو، وضعیت تأیید، سهم فروش، و دکمه‌های سریع
  («ارسال طرح تازه» → `?tab=upload`، «میز کار فروش» → `?tab=assets`، «پروفایل عمومی»).
- نوار راه‌اندازی استودیو: پنج گام (پرونده‌ی استودیو، اولین طرح، چند رنگ‌بندی، فرمت‌های کامل، اطلاعات تسویه).
- شش شاخص کلیدی: درآمد ۳۰ روز، فروش، دانلود، بازدید، سهم شما، نرخ تبدیل — با فلش روند نسبت به ۳۰ روز قبل
  (از همان API تحلیل، پنجره‌ی ۶۰ روزه منهای ۳۰ روز جاری).
- وضعیت آثار (منتشرشده/در بازبینی/رد شده)، کیف پول (قابل برداشت، کل، پرداخت‌شده، در انتظار، حداقل تسویه) و
  نمودار درآمد روزانه + پرفروش‌ترین آثار.
- **سرانه‌ی تحویل**: تعداد رنگ‌بندی، تعداد فایل، حجم کل (فارسی‌سازی‌شده) و پخش فرمت‌ها روی آثار.
- **پیشنهاد فروش بیشتر**: چند اثر فرمت AI/PSD ندارد، چند اثر تک‌رنگ است، چند اثر پیش‌نمایش ندارد.
- جدول آثار: پیش‌نمایش، دسته، سواچ رنگ‌ها، چیپ فرمت‌ها، حجم تحویل، بازدید/فروش/قیمت پایه، وضعیت، یادداشت
  بازبینی و نشان «فایل تازه — بازبینی دوباره».
- آخرین تراکنش‌های دفتر مالی و ترکیب فروش بر پایه‌ی نوع لایسنس (+ خلاصه‌ی کدهای معرف در صورت وجود).

**خروج از حساب** — یک کنترل واحد (`src/components/profile/SignOutButton.tsx`) در چهار حالت
(پر · خط‌دار · متنی · ردیف منو) و یک حالت آیکونی؛ در هدر (منوی آواتار و منوی موبایل)، در سه جای صفحه‌ی
حساب (سربرگ پروفایل، انتهای منوی کنار، و ردیف مخصوص در «تنظیمات») و در داشبورد هنرمند، میز کار فروش و
پورتفولیو. با فشردن آن، کوکی نشست پاک می‌شود، تاریخچه **replace** می‌شود (تا دکمه‌ی Back به صفحه‌ی خصوصی
برنگردد) و روتر refresh می‌شود تا همه‌ی کامپوننت‌های سرور به‌صورت مهمان رندر شوند.

**پورتفولیو** — همان داشبورد قدیمی (الگوها، محصولات، پروفایل، آمار) دست‌نخورده به
`/{locale}/artist/portfolio` منتقل شد و از داشبورد و میز کار لینک دارد؛ عنوانش هم «پورتفولیو و پروژه‌ها»
شد تا با داشبورد اشتباه نشود. درِ میز کار (`/artist/marketplace`) با `?tab=` قابل لینک است.

**دسترسی** — میدل‌ور: کاربر بدون نشست به `/login?next=…` می‌رود؛ خریدارِ وارد‌شده وارد `/artist` می‌شود و
صفحه‌ی صادقانه‌ی «اینجا خانه‌ی هنرمندان است» با دکمه‌ی ثبت‌نام فروشنده را می‌بیند (APIهای هنرمند همچنان
نقش را خودشان بررسی می‌کنند).

## ۶٫۷) آپلود تأییدشده و انتشار فوری در فروشگاه (تازه)

**مشکل گزارش‌شده:** هنرمند اثر را آپلود می‌کرد، فرم «ثبت شد» نشان می‌داد و او را به دسته‌ی انتخابی
فروشگاه می‌برد، ولی اثر آنجا نبود. علت‌ها: (۱) `/shop` فقط محصولات فیزیکی محتوای سایت را نشان می‌داد و
آثار هنرمندان هرگز در آن فهرست نمی‌شدند (حتی پس از تأیید)؛ (۲) هر اثر تازه تا تأیید مدیر خصوصی می‌ماند؛
(۳) هیچ بررسی‌ای روی کامل بودن فایل‌ها وجود نداشت و فرم پاسخ‌های غیر JSON (مثلاً صفحه‌ی خطای پروکسی) را
به‌شکل خطای نامفهوم نشان می‌داد؛ (۴) پیش‌نمایش آثار خصوصی در استودیو، داشبورد و صف بازبینی (به‌خاطر
بهینه‌ساز تصویر Next که کوکی ندارد) خطای ۴۰۰ می‌داد.

**اکنون:**
- **یکپارچگی**: مرورگر برای هر فایل (تا ۶۴ مگابایت) و برای هر قطعه sha256 می‌فرستد. سرور قطعه‌ی ناقص را
  با `422 part_size_mismatch` و قطعه‌ی خراب را با `422 part_corrupted` رد می‌کند؛ `complete` فایل را فقط
  وقتی می‌سازد که حجم (`size_mismatch`) و چک‌سام (`checksum_mismatch`) دقیقاً بخواند. در S3 اندازه‌ی واقعی
  قطعه در باکت بررسی می‌شود. هر درخواست `complete` ابتدا جلسه را قفل می‌کند (`completing`) تا تلاش دوباره‌ی
  مرورگر اثر تکراری نسازد.
- **مرورگر**: قطع اینترنت، ۴۲۹ و ۵xx تا ۴ بار با فاصله‌ی فزاینده دوباره تلاش می‌شود؛ پیشرفت واقعی بایت‌ها
  (XHR) نمایش داده می‌شود؛ هر جایگاه فقط وقتی «کامل رسید ✓» می‌شود که سرور همان حجم و چک‌سام را تأیید کند؛
  اگر وسط کار خطا رخ دهد، دکمه «ادامه‌ی آپلود» می‌شود و فقط فایل‌های باقی‌مانده را می‌فرستد.
- **نهایی‌سازی** — `POST /api/marketplace/upload/finalize`
  `{assetId, files:[{colourwayId, formatId, sizeBytes, sha256?}]}`: سرور فهرست مرورگر را با آنچه واقعاً
  ذخیره کرده (وجود فایل، حجم، چک‌سام، و سالم بودن شیء ذخیره‌شده) مقایسه می‌کند. هر مغایرت →
  `409 incomplete_upload` با فهرست مشکلات؛ اثر خصوصی می‌ماند و مرورگر فقط همان فایل‌ها را دوباره می‌فرستد.
  اثری که هنوز نهایی نشده `uploadState: "uploading"` دارد و در استودیو «آپلود ناتمام» و در صف بازبینی
  هشدار «آپلود نهایی نشده» نشان داده می‌شود.
- **سیاست انتشار** (مدیر → بازارچه → تنظیمات → «انتشار آثار هنرمندان»):
  - «انتشار فوری پس از آپلود کامل» (پیش‌فرض روشن؛ `MARKETPLACE_AUTO_PUBLISH=0` پیش‌فرض را خاموش می‌کند
    و مقدار ذخیره‌شده در پنل همیشه مقدم است): اثر بلافاصله `approved` + `public` می‌شود و همان ایمیل
    «اثر شما تأیید شد» برای هنرمند در صف ارسال قرار می‌گیرد؛
  - خاموش: اثر در صف بازبینی می‌ماند؛ «تأیید خودکار طرح‌های بی‌درز» (که قبلاً بی‌اثر بود) در این حالت
    آثار بی‌درز را خودکار منتشر می‌کند؛
  - فایلی که اسکنر مشکوک بداند همیشه منتظر مدیر می‌ماند؛ اثر رد شده یا پنهان‌شده توسط مدیر هرگز خودکار
    منتشر نمی‌شود؛ مدیر هر زمان می‌تواند اثر منتشرشده را پنهان یا رد کند.
- **فروشگاه**: آثار منتشرشده (`approved` + `public` + حداقل یک لایسنس فعال) در `/shop` زیر همان دسته‌ای
  که هنرمند انتخاب کرده، کنار محصولات آن دسته نمایش داده می‌شوند (کارت `WorkCard` با برچسب «فایل دیجیتال»،
  قیمت «از …»، فرمت‌ها و رنگ‌ها، با لینک به `/marketplace/<slug>`، بدون سبد خرید فیزیکی) و در شمارنده‌ی
  دسته‌ها، فیلتر «سازنده»، کل نتایج و اعداد هیرو شمرده می‌شوند. فیلتر `?category=` (دسته‌ی فیزیکی) فقط
  محصولات را نشان می‌دهد.
- **فرم آپلود** فقط پاسخ `finalize` را گزارش می‌کند: «کامل آپلود و منتشر شد» با لینک صفحه‌ی اثر و دسته (و
  انتقال خودکار به دسته پس از ۸ ثانیه، با امکان ماندن)، یا «در صف بازبینی است» با دکمه‌ی «آثار من»؛ و
  دکمه‌ی «آپلود اثر دیگر».
- پیش‌نمایش‌های خصوصی با `unoptimized` مستقیم از `/api/marketplace/media` (با کوکی مالک/مدیر) بارگذاری
  می‌شوند؛ تصاویر پیش‌فرض آواتار و کاور هنرمندان تازه (`public/images/artists/*placeholder.jpg`) اضافه شد.

تست: `bash scripts/marketplace-smoke/upload-publish-e2e.sh` (آپلود تک‌درخواستی و چندبخشی، رد قطعه‌ی ناقص و
خراب، رد چک‌سام نادرست، `incomplete_upload`، انتشار فوری، نمایش در `/shop?family=…`، حالت بازبینی).

## ۷) مسیرها و صفحات تازه

| مسیر | کار |
|---|---|
| `/{locale}/marketplace` | ویترین دیجیتال (فیلتر نوع، جست‌وجو، مرتب‌سازی، سبد) |
| `/{locale}/marketplace/[slug]` | صفحه اثر با پیش‌نمایش واترمارک‌شده، لیست لایسنس‌ها و شرایط استفاده |
| `/{locale}/marketplace/cart` | پرداخت دیجیتال (کد تخفیف، انتخاب درگاه، فاکتور زنده) |
| `/{locale}/checkout/return` | رسید سفارش با ریز صورتحساب (اقلام، تخفیف، مالیات، قابل پرداخت) + لایسنس‌ها + لینک دانلود و گواهی |
| `/{locale}/checkout/sandbox` | صفحه‌ی درگاه آزمایشی |
| `/{locale}/marketplace/subscriptions` | پلن‌های اشتراک و پنل پاس فعال |
| `/{locale}/account` | حساب کاربری خریدار — برای هنرمند/طراح: **خودِ داشبورد هنرمند** (بدون بخش‌های حساب) |
| `/{locale}/account/licenses` | گنجینه‌ی لایسنس‌ها (گواهی، دانلود، سهمیه، لینک راستی‌آزمایی) |
| `/{locale}/verify` و `/{locale}/verify/[serial]` | راستی‌آزمایی عمومی گواهی (هدف QR) |
| `/{locale}/delivery/[token]` | لینک تحویل ارسالی با ایمیل |
| `/{locale}/artist` | **داشبورد هنرمند**: شاخص‌های فروش، وضعیت آثار، سرانه‌ی تحویل، کیف پول و پیشنهادها |
| `/{locale}/artist/portfolio` | مدیر پورتفولیو (الگوها، محصولات، پروفایل، آمار) — همان محتوای قبلی، مسیر تازه |
| `/{locale}/signup` | ثبت‌نام خریدار: فرم کوتاه چهارفیلدی |
| `/{locale}/creators/join` | ثبت‌نام هنرمند/طراح: صفحه‌ی فروشندگان + فرم تک‌صفحه‌ای خودش |
| `/{locale}/artist/marketplace` | استودیوی هنرمند: آثار، بارگذاری، کیف پول، تحلیل، کد معرف (با `?tab=` قابل لینک) |
| `/admin/{locale}/marketplace` | کنسول مدیر: داشبورد، صف بررسی، سفارش‌ها، تسویه‌ها، صندوق ایمیل، یکپارچه‌سازی، تنظیمات |
| `src/app/api/marketplace/**` | حدود ۳۰ روت‌هندلر (آپلود، کاتالوگ، پرداخت، دانلود، لایسنس، گواهی، اشتراک، هنرمند، ادمین، راستی‌آزمایی) |

---

## ۸) امنیت و داده

- **فایل مادر هرگز عمومی نیست**؛ فقط مشتق‌های واترمارک‌شده سرو می‌شوند و کلیدهای master در API مدیا رد
  می‌شوند (`403`).
- حقوق هر روت‌هندلر جدا بررسی می‌شود (`requireAdmin` / `requireArtistOrAdmin` / مالک سفارش / توکن امضاشده)
  و پیام‌های خطا عمداً مبهم‌اند (پاسخ `404` برای کسی که حق ندارد، تا وجود یک سریال لو نرود).
- Rate limit روی مسیرهای حساس: آپلود ۴۰۰ درخواست/۱۰ دقیقه، چک‌اوت ۳۰/۱۵ دقیقه، کد تخفیف ۶۰/۱۵ دقیقه،
  اعمال کد ۶/ساعت، ورود با throttle قبلی سایت.
- شمارش دانلود، ویرایش قیمت، تأیید/رد، تسویه و بازپرداخت همه در دفتر کل و تاریخچه ثبت می‌شوند.
- گواهی عمومی فقط حداقل اطلاعات را نشان می‌دهد: نام خریدار **مخفی** (`س. م.`)، بدون مبلغ و بدون فایل.

---

## ۹) تست گام‌به‌گام (چیزی که واقعاً اجرا و تأیید شد)

راه‌اندازی محلی:

```bash
npm install
ADMIN_EMAIL=admin@… ADMIN_PASSWORD=… AUTH_SECRET=… npm run dev      # یا
npm run build && node dist/.next/standalone/server.js
```

اسکریپت‌های آماده‌ی تست دودی (همه در همین مخزن، بدون نیاز به فریمورک تست):

```bash
DATA=dist/.next/standalone/data node scripts/marketplace-smoke/seed-artist-user.mjs   # ساخت هنرمند محلی
bash scripts/marketplace-smoke/http-e2e.sh                   # آپلود → بررسی → خرید → دانلود → گواهی
bash scripts/marketplace-smoke/exclusive-e2e.sh              # فروش انحصاری و بازپرداخت
bash scripts/marketplace-smoke/artist-e2e.sh                 # قیمت‌گذاری، فروش، تسویه، کد معرف
bash scripts/marketplace-smoke/sub-e2e.sh                    # پلن اشتراک و دانلود پوشش‌داده‌شده
bash scripts/marketplace-smoke/pages.sh                      # رندر همه‌ی صفحات جدید
bash scripts/marketplace-smoke/multipart-e2e.sh             # آپلود چندبخشی فایل بزرگ (نیاز به تریگر زیر)
bash scripts/marketplace-smoke/upload-publish-e2e.sh        # آپلود تأییدشده → انتشار فوری → نمایش در دسته‌ی فروشگاه
```

برای تست مسیر چندبخشی با فایل کوچک، آستانه را پایین بیاورید و سرور را با آن اجرا کنید:
`MARKETPLACE_MULTIPART_THRESHOLD_MB=5 node dist/.next/standalone/server.js` — اسکریپت با یک بافت
۱۲ مگابایتی، دو قطعه می‌فرستد و بایت‌به‌بایت بودن فایل مونتاژشده و فایل تحویلی را بررسی می‌کند.

مسیر پیشنهادی تست دستی (همه با داده‌ی واقعی اجرا شده است):

1. **ورود ادمین** → `/admin/fa/marketplace` (اگر نشست نداشته باشید، به `/admin/login` هدایت می‌شوید).
2. **آپلود** → `POST /api/marketplace/upload/session` با
   `{filename,sizeBytes,mime,title,familyId,formatId,colourwayId,colourway:{name,hex},attachToAssetId,…}`؛
   `familyId` یکی از هشت شناسه‌ی `src/lib/data/families.ts` است (وگرنه `invalid_family` ۴۰۰)، `formatId`
   یکی از هفت فرمت بالا است (اگر نیاید از نام فایل حدس زده می‌شود) و `attachToAssetId` فایل را به رنگ
   دیگری از همان اثر می‌چسباند؛ سپس قطعه‌ها به `/upload/part` و پایان با `/upload/complete`، و پس از
   آخرین فایل `POST /upload/finalize` که کل دسته را بررسی و (با انتشار فوری) منتشر می‌کند.
   فرم آپلود هنرمند هر دو را الزامی کرده و پس از انتشار، هنرمند خودکار به `/shop?family=<slug>` می‌رود
   که اثر اکنون آنجاست. (تست: PNG کاشی‌پذیر ۸۴۵٬۰۷۶ بایتی → امتیاز بی‌درز
   **۰.۹۸۲**، پنج موکاپ، اسکن `clean`.)
3. **تأیید** (فقط در حالت بازبینی؛ با انتشار فوری، `finalize` این کار را کرده است) →
   `POST /api/marketplace/admin/review {action:"approve",assetId,publish:true}` → اثر در
   `GET /api/marketplace/assets` و `/shop?family=<slug>` ظاهر می‌شود.
4. **قیمت‌گذاری** → `PATCH /api/marketplace/artist/assets` با لیست tiers.
5. **پیش‌فاکتور** → `POST /api/marketplace/checkout/quote` →
   `۳۹۰۰۰۰۰ + ۹٪ مالیات = ۴۲۵۱۰۰۰ تومان` (و با کد تخفیف ۱۰٪: `۲۷۰۰۰۰۰ + ۲۴۳۰۰۰ − ۲۷۰۰۰۰`).
6. **خرید** → `POST /api/marketplace/checkout` → پاسخ `{payment:{redirectUrl,sandbox:true}}` → صفحه‌ی
   درگاه آزمایشی → پرداخت → `GET /api/marketplace/payments/sandbox?authority=…` و سپس
   `POST` همان آدرس با `{outcome:"paid"}` → لایسنس `RA-LIC-2026-000001`.
7. **دانلود** → `GET /api/marketplace/orders/<id>` برای هر فایل (رنگ × فرمت) توکن تازه می‌دهد؛ هر
   دانلود **بایت‌به‌بایت برابر** با همان فایل آپلودشده است، سهمیه دست‌کم به تعداد فایل‌ها می‌رسد، توکن
   دست‌کاری‌شده `invalid_token` و توکن مجاز برای فایلی بیرون از اثر هم `invalid_token` می‌گیرد.
8. **گواهی** → `GET /api/marketplace/licenses/<id>/certificate?locale=fa` → PDF دو صفحه‌ای (~۴۶۶ کیلوبایت)
   با متن فارسی صحیح (بازبینی بصری شد).
9. **راستی‌آزمایی** → `GET /api/marketplace/verify/RA-LIC-2026-000001` → `valid:true` و نام خریدار مخفی.
10. **انحصاری** → فروش لایسنس انحصاری → اثر `sold_exclusive` + خصوصی و فهرست صفر؛ بازپرداخت ادمین →
    لایسنس `revoked` و اثر به فروشگاه برگشت (`approved/public`).
11. **تسویه** → فروش ۵٬۰۰۰٬۰۰۰ تومانی با سهم ۴۰٪ → کیف پول هنرمند ۲٬۰۰۰٬۰۰۰؛ درخواست تسویه → تأیید →
    پرداخت → `available: 0`, `paidOut: 2٬000٬000`.
12. **اشتراک** → خرید «پاس استودیو» (۲٬۹۰۰٬۰۰۰ + ۹٪ = ۳٬۱۶۱٬۰۰۰) → پاس فعال ۱۵/۱۵ → دانلود پوشش‌داده‌شده
    (سفارش صفر-مبلغ + لایسنس) → سهمیه `used: 1, remaining: 14` → لغو اشتراک.
13. **کد معرف** → ساخت `NILOUFAR15` توسط هنرمند → بازدید `/{locale}/marketplace?ref=NILOUFAR15` سه بار →
    شمارش کلیک `۳` و کد نامعتبر بدون ثبت.

---

**ریگ‌های تازه (روی همین بیلد اجرا و سبز شدند):**

- `bash scripts/marketplace-smoke/artist-dashboard-e2e.sh` — دو درِ ثبت‌نام و داشبورد هنرمند:
  `/{locale}/signup` فرم خریدار است (چهار فیلد، بدون هیچ فیلد فروشندگی) و `/{locale}/creators/join` صفحه‌ی
  هنرمند/طراح با فرم تک‌صفحه‌ای خودش (فقط چهار فیلد حساب اجباری، بدون هیچ گام)، ثبت‌نام واقعی همان پنج مقدار
  فروشنده را روی رکورد هنرمند ذخیره می‌کند و برای فیلدهای خالی چیزی از خودش نمی‌سازد، مدیر پرونده را می‌بیند و
  تأیید می‌کند، `/artist` برای هنرمند شاخص‌ها/آثار/تحویل/کیف پول را نشان می‌دهد و برای خریدار صفحه‌ی راهنما، و
  `/artist/portfolio` همان مدیر پورتفولیو را سرو می‌کند.
- `bash scripts/marketplace-smoke/formats-e2e.sh` — دو رنگ‌بندی × هفت فرمت روی یک اثر، رد شدن‌های
  (invalid_format · unsupported_type · raster_required · invalid_signature)، خرید و **۱۲ دانلود
  بایت‌به‌بایت برابر**، سهمیه‌ی لایسنس، توکن جعلی و کلید خام `private/masters/…`.

## ۱۰) پیکربندی (`.env.example` کامل به‌روز شده است)

هیچ‌کدام اجباری نیستند؛ هر بلوک فقط یک یکپارچه‌سازی را روشن می‌کند و در نبودش حالت پیش‌فرض کاری خودش
فعال است (ذخیره‌سازی محلی، درگاه آزمایشی، اسکنر داخلی، outbox ایمیل).

- `MARKETPLACE_S3_*` — باکت خصوصی (AWS/R2/Arvan/Wasabi/…)، در نبودش `data/objects`.
- `MARKETPLACE_MAX_MASTER_MB`, `MARKETPLACE_MULTIPART_THRESHOLD_MB` (خالی = محلی: یک قطعه، S3: ۲۰۰),
  `MARKETPLACE_PART_SIZE_MB`.
- `MARKETPLACE_AUTO_PUBLISH` — پیش‌فرض `1`: انتشار فوری آثار پس از آپلود کامل؛ `0`: بازبینی پیش از انتشار
  (تنظیم ذخیره‌شده در پنل مدیر مقدم است).
- `MARKETPLACE_VAT_PCT`, `MARKETPLACE_ARTIST_SHARE_PCT`, `MARKETPLACE_AFFILIATE_PCT`,
  `MARKETPLACE_AFFILIATE_DISCOUNT_PCT`, `MARKETPLACE_MIN_PAYOUT_TOMAN/USD`.
- `MARKETPLACE_DOWNLOAD_TTL_S`, `MARKETPLACE_EMAIL_LINK_TTL_S`.
- `ZARINPAL_MERCHANT_ID` (+`ZARINPAL_BASE_URL`), `STRIPE_SECRET_KEY`.
- `CLAMAV_HOST`/`CLAMAV_PORT`, `RESEND_API_KEY` یا `SMTP_*`, `MARKETPLACE_EMAIL_FROM`,
  `MARKETPLACE_ADMIN_EMAIL`, `MARKETPLACE_ARTIST_NOTIFY_EMAIL`, `MARKETPLACE_NOTIFY_ARTISTS`.

---

## ۱۱) محدودیت‌ها و کارهای باقی‌مانده (صادقانه)

1. **زرین‌پال شبیه‌سازی‌شده است** (به‌خواست شما) — پیش از پذیرش پرداخت واقعی، مرچنت‌آی‌دی و
   `ZARINPAL_BASE_URL` را بگذارید و یک خرید واقعی کوچک را تست کنید.
2. **ClamAV و S3 واقعی روی این محیط بالا نبودند**؛ کد مسیرها نوشته و تست منطقی شده‌اند، اما اتصال
   واقعی به clamd و یک باکت S3 باید روی سرور شما یک‌بار تأیید شود.
3. **تمدید خودکار اشتراک** عمداً پیاده نشده (پرداخت خودکار بدون درگاه واقعی امن نیست)؛ پاس پس از پایان
   دوره با اولین دانلود تازه‌سازی می‌شود و در نبود پرداخت منقضی می‌گردد.
4. **بک‌اند فایل محلی برای پروداکشن کافی نیست** (سیستم‌فایل سرورلس فقط-خواندنی است)؛ برای دیپلوی واقعی
   S3 (یا Upstash) لازم است — همان‌طور که برای محتوای سایت هم از قبل لازم بود.
5. پیش از انتشار، توصیه می‌شود: یک خرید واقعی با مبلغ کم، یک آپلود >۲۰۰ مگابایت روی S3، و یک ارسال
   ایمیل واقعی تست شود. در این محیط مسیر چندبخشی روی بک‌اند فایل محلی با فایل ۱۲ مگابایتی تست شده است؛
   فقط presign خود S3 تأیید نشده.
6. `npm run check` (typecheck + lint) در وضعیت فعلی سبز است: صفر خطا و ۴۸ هشدار از پیش موجود.
