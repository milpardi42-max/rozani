# گزارش کار — دکمه‌ی خروج از حساب

**کامیت‌ها:** `ae3d720` + `1780b29` روی شاخه‌ی `arena/01a0cf92-rozvelt` · **PR:** [#1](https://github.com/milpardi42-max/Rozvelt/pull/1)

## خواسته‌ی شما

> «برای خروج هم از پروفایل یک دکمه خروج قرار بده تا کاربر بتونه از حساب خارج بشه.»

قبلاً فقط یک دکمه‌ی کوچک در سربرگ صفحه‌ی `/account` بود و در بقیه‌ی جاها هیچ راهی برای خروج نبود. حالا یک
کنترل واحد و حرفه‌ای در همه‌ی نقاط ورود کاربرِ وارد‌شده گذاشته شد.

## کنترل جدید

`src/components/profile/SignOutButton.tsx` — یک دکمه برای کل سایت، با چهار حالت نمایش:
**پر (solid)** · **خط‌دار (outline)** · **متنی (ghost)** · **ردیف منو (menu)** و یک حالت **فقط آیکون**.
همه‌ی حالت‌ها وضعیت «در حال خروج…» با اسپینر دارند و لیبل فارسی/انگلیسی‌شان خودکار است.

## کجاها اضافه شد

| محل | شکل نمایش |
|---|---|
| **هدر سایت (دسکتاپ)** | کلیک روی آواتار یک منوی حساب باز می‌کند: حساب من · گواهی‌ها و دانلودها · داشبورد هنرمند · میز کار فروش — و در پایان **«خروج از حساب»**. با کلیک بیرون یا `Esc` بسته می‌شود. |
| **هدر (موبایل)** | کارت کاربر (نام + ایمیل) با دکمه‌ی خروج در انتهای منوی موبایل |
| **صفحه‌ی پروفایل `/account`** | ۱) دکمه‌ی سربرگ کنار نام کاربر ۲) ردیف «خروج از حساب» در انتهای منوی کناری ۳) ردیف مخصوص در تب **«تنظیمات»** با توضیح «نشست این دستگاه بسته می‌شود؛ لایسنس‌ها و دانلودها در حساب می‌مانند» |
| **گنجینه‌ی لایسنس‌ها `/account/licenses`** | همان سربرگ پروفایل |
| **داشبورد هنرمند `/artist`** | کنار دکمه‌های سریع (به‌همراه «حساب من») |
| **میز کار فروش `/artist/marketplace`** | بالای صفحه، کنار عنوان |
| **پورتفولیو `/artist/portfolio`** | در نوار بالای صفحه |

## رفتار فنی (چرا فقط پاک‌کردن کوکی کافی نیست)

با کلیک، سه کار انجام می‌شود:

1. `logout()` — وضعیت کلاینت پاک و `POST /api/auth/logout` صدا زده می‌شود (کوکی نشست پاک می‌شود).
2. `router.replace("/")` — چون از **replace** استفاده می‌شود، دکمه‌ی Back مرورگر به صفحه‌ی خصوصی برنمی‌گردد.
3. `router.refresh()` — همه‌ی کامپوننت‌های سرور (داشبورد، میز کار، پروفایل) دوباره به‌صورت **مهمان** رندر
   می‌شوند؛ بدون این کار یک صفحه‌ی کش‌شده می‌توانست اطلاعات نشست قبلی را نشان بدهد.

## تأیید زنده (روی بیلد پروداکشن)

بخش تازه‌ی **«8. signing out»** در ریگ `scripts/marketplace-smoke/artist-dashboard-e2e.sh` — همه سبز:

- `/fa/artist` · `/fa/artist/marketplace` · `/fa/artist/portfolio` · `/en/artist` کنترل خروج را سرو می‌کنند.
- `/fa/account` و `/fa/account/licenses` (کامپوننت کلاینت) کنترل را در بسته‌ی جاوااسکریپت همان صفحه دارند.
- بسته‌ی هدر، منوی حساب («حساب من») و ردیف خروج را دارد.
- نشست قبل از خروج کار می‌کند (`/api/auth/me` کاربر را برمی‌گرداند) → `POST /api/auth/logout` پاسخ `ok` →
  بعد از آن `/api/auth/me` مقدار `null` می‌دهد و `/fa/artist` به‌جای ۲۰۰، کد **۳۰۷** (هدایت به ورود) می‌دهد.

بقیه‌ی ریگ‌ها هم دوباره سبز شدند: `http` · `exclusive` · `artist` · `sub` · `family` · `formats` ·
`multipart` · `pages` · ثبت‌نام آکادمی. `tsc` پاک و `eslint` صفر خطا.

## برای دیدن در پیش‌نمایش

سرور بالاست. با `buyer@example.com` / `buyer-dev-pass` وارد شوید و بعد:

- روی **آواتار در هدر** بزنید → منوی حساب، آخرین ردیف «خروج از حساب».
- `/fa/account` → دکمه‌ی سربرگ، ردیف انتهای منوی کناری، و تب «تنظیمات».
- با `niloufar@example.com` / `artist-dev-pass` → داشبورد هنرمند و میز کار فروش هم دکمه‌ی خروج دارند.
