# گزارش کار — فرمت‌های تحویل و رنگ‌بندی طرح‌ها

**تاریخ:** ۱۴۰۵/۰۷/۰۲ · **کامیت:** `bdd2d2a` روی شاخه‌ی `arena/01a0cf92-rozvelt` · **PR:** [#1](https://github.com/milpardi42-max/Rozvelt/pull/1)

## خواسته‌ی شما

> «آپلود طرح‌ها و دانلود خریدارها باید خیلی حرفه‌ای باشد — PNG/JPG، تصویر پیش‌نمایش، AI، PSD، SVG،
> EPS. و هنرمند باید در **زمان آپلود محصول** بتواند از هر طرح چند رنگ انتخاب کند و همان‌ها را آپلود کند.»

هر دو طرف انجام شد: **زمان آپلود** هنرمند رنگ و فرمت را انتخاب می‌کند، **زمان دانلود** خریدار همه‌ی
فایل‌های همه‌ی رنگ‌ها را می‌گیرد — به‌همراه پیش‌نمایش تصویری.

## ۱) چه فرمت‌هایی و چگونه

هفت فرمت تعریف شد و `src/lib/marketplace/formats.ts` تنها منبع حقیقت است — همان‌جا که آپلود اعتبارسنجی
می‌شود، همان‌جا فروشگاه نمایش می‌دهد و همان‌جا دانلود بررسی می‌کند:

| فرمت | نقش | حداقل حجم | اثبات واقعی بودن فایل |
|---|---|---|---|
| PNG | تحویل تصویری با شفافیت | ۱ کیلوبایت | امضای `\x89PNG` |
| JPG | تحویل سبک برای چاپ دیجیتال | ۱ کیلوبایت | `FF D8 FF` |
| تصویر پیش‌نمایش | کاور فروشگاه | ۱ کیلوبایت | PNG/JPG/WebP |
| AI | فایل منبع ایلاستریتور | ۶۴ بایت | `%PDF` یا `%!PS` |
| PSD | فایل لایه‌دار فتوشاپ | ۶۴ بایت | `8BPS` |
| SVG | وکتور باز وب | ۶۴ بایت | وجود `<svg` |
| EPS | فایل چاپ حرفه‌ای | ۶۴ بایت | `%!PS` |

فایل باید **واقعاً** همان فرمتی باشد که ادعا می‌کند: اول پسوند/MIME با اسلات انتخابی مقایسه می‌شود،
بعد بایت‌های امضا خوانده می‌شود و در پایان اسکن ویروس انجام می‌شود.

> **یک باگ واقعی که تست زنده پیدا کرد:** حداقل حجم آپلود یک عدد ثابت ۱ کیلوبایتی بود و فایل‌های واقعی
> AI (۸۷۱ بایت)، SVG (۴۸۶ بایت) و EPS (۲۲۶ بایت) را با پیام «فایل خیلی کوچک است» رد می‌کرد. حالا حداقل
> حجم مخصوص هر فرمت است (رستر ۱ کیلوبایت، وکتور ۶۴ بایت) و امضای فایل، اثبات اصلی است.

## ۲) سمت هنرمند: انتخاب رنگ در زمان آپلود

فرم آپلود به چهار گام تبدیل شد:

1. **دسته‌ی محصول** — همان هشت خانواده‌ی قبلی (الگوها ← کاغذ دیواری … آثار هنری دیواری).
2. **اطلاعات اثر** — عنوان، توضیح، برچسب، قیمت‌ها.
3. **کارت‌های رنگ** — هر کارت یک رنگ است (۱۲ رنگ آماده یا کد hex دلخواه) و برای همان رنگ هفت اسلات
   فایل دارد: PNG · JPG · پیش‌نمایش · AI · PSD · SVG · EPS. هر اسلات وضعیت خودش را نشان می‌دهد
   (در صف / در حال آپلود / انجام‌شده / خطا) و رنگ بعدی به‌صورت خودکار به همان اثر می‌چسبد.
4. **خلاصه و ارسال** — تعداد رنگ، فرمت‌ها و حجم کل؛ پس از موفقیت، هنرمند خودکار به دسته‌ی همان اثر می‌رود.

قواعد حرفه‌ای که رعایت شد:

- **شروع هر کار با فایل رستر** (PNG یا JPG): پیش‌نمایش، بندانگشتی، کاشی بی‌درز و امتیاز بی‌درزی از آن
  ساخته می‌شوند؛ اگر هنرمند اول SVG بگذارد، پیام روشن `raster_required` می‌گیرد.
- **فایل نامربوط** در اسلات اشتباه → `unsupported_type` (۴۱۵)، **محتوای جعلی** (مثلاً بایت‌های PNG با
  نام `.psd`) → `invalid_signature` (۴۲۲) و آن فایل از سرور پاک می‌شود.
- **رنگ تازه روی اثری که تأیید شده بود**، اثر را به `pending_review` برمی‌گرداند تا مدیر دوباره ببیند.
- اثرهای قدیمی که رنگ‌بندی ندارند، خودکار یک رنگ «اصلی/Original» می‌گیرند؛ هیچ چیزی نمی‌شکند.

## ۳) سمت خریدار: همه‌چیز در یک لایسنس

- یک لایسنس، **همه‌ی فایل‌های همه‌ی رنگ‌ها** را باز می‌کند؛ سهمیه‌ی دانلود هرگز کمتر از تعداد فایل‌های
  تحویل نیست (۱۲ فایل ⇒ ۱۲ دانلود).
- هر فایل **توکن امضاشده‌ی جداگانه** دارد. توکن دست‌کاری‌شده یا توکن معتبری که به فایلی بیرون از همان
  اثر اشاره کند، `invalid_token` می‌گیرد.
- نام فایل‌ها خوانا و یکتاست: `Quiet-Garden-Crimson-PSD.psd` (اثر، رنگ، فرمت).
- فهرست کامل فایل‌ها در: «سفارش‌های من» · پنل لایسنس‌ها · ایمیل تحویل · صفحه‌ی لینک ایمیل‌شده‌ی
  `/delivery/<token>` (با حجم و لینک هر فایل) · گواهی PDF. فایل مادر هم مثل قبل دانلود می‌شود.
- پیش‌نمایش عمومی همیشه **واترمارک‌دار** است؛ کلید خام `private/masters/…` از مسیر عمومی media سرو
  نمی‌شود (پاسخ ۴۰۳).

## ۴) فروشگاه و پنل مدیر

- **صفحه‌ی اثر:** سواچ رنگ‌ها، گالری هر رنگ در کنار پیش‌نمایش‌های تولیدشده و جدول «فرمت‌های تحویل» با
  حجم هر فرمت.
- **کارت کاتالوگ:** نام خوانای فرمت‌ها (PNG · PSD · EPS) و نقطه‌های رنگ.
- **صف بازبینی مدیر:** رنگ‌ها، فرمت‌ها و حجم کل تحویل در جعبه‌ی «تحویل نهایی».

## ۵) تأیید زنده (نه فقط ۲۰۰ گرفتن)

ریگ `bash scripts/marketplace-smoke/formats-e2e.sh` روی بیلد پروداکشن و با فایل‌های واقعی (ساخته‌شده با
`scripts/marketplace-smoke/mkformats.mjs` روی sharp/pdf-lib) اجرا شد — **همه‌ی بررسی‌ها سبز**:

- **۱۴ آپلود روی یک اثر** (۲ رنگ × ۷ اسلات) — همه با یک شناسه‌ی اثر، رنگ و فرمت درست.
- رد شدن‌ها: فرمت نامعلوم `invalid_format`، پسوند ناهمخوان `unsupported_type`، شروع با SVG
  `raster_required`، PNG با نام `.psd` → `invalid_signature`.
- فروشگاه: بلوک «فرمت‌های تحویل»، هر دو نام رنگ، پیش‌نمایش هر رنگ، نام خوانای فرمت‌ها و سواچ‌ها.
- خرید واقعی: پیش‌فاکتور با ۹٪ مالیات → درگاه آزمایشی → لایسنس `RA-LIC-2026-000004`. رسید **۱۲ فایل** با
  لینک جداگانه، سهمیه ۱۲، پنل لایسنس‌ها همان ۱۲ فایل، صفحه‌ی ایمیل‌شده‌ی تحویل هم فهرست کامل.
- **۱۲ دانلود از ۱۲، بایت‌به‌بایت برابر** با فایل‌های آپلودشده:

| رنگ | PNG | JPG | AI | PSD | SVG | EPS |
|---|---|---|---|---|---|---|
| زرشکی `#be123c` | ۵٬۸۲۶ ✔ | ۱۵٬۴۴۴ ✔ | ۸۷۲ ✔ | ۷۸۶٬۴۷۲ ✔ | ۴۸۶ ✔ | ۲۲۶ ✔ |
| فیروزه‌ای `#0d9488` | ۵٬۸۲۶ ✔ | ۱۵٬۴۴۴ ✔ | ۸۷۲ ✔ | ۷۸۶٬۴۷۲ ✔ | ۴۸۶ ✔ | ۲۲۶ ✔ |

- امنیت: توکن دست‌کاری‌شده `invalid_token` · توکن مجاز اثر دیگر `invalid_token` · چسباندن فایل به اثری
  که مالکش نیستی `asset_not_found` · کلید خام `private/masters/…` روی media → ۴۰۳.
- ریگ‌های قبلی با قرارداد تازه دوباره سبز شدند: `http-e2e` · `artist-e2e` · `multipart-e2e` (آپلود
  چندبخشی ۱۲ مگابایتی هنوز بایت‌به‌بایت) · `family-e2e` (هشت خانواده) · `pages.sh` · ثبت‌نام آکادمی.
- `npx tsc --noEmit` پاک · `eslint` صفر خطا · `npm run build` سبز و داده‌ی زنده بعد از build دست‌نخورده
  (روی همین بیلد، سرور در حال اجراست).

## ۶) ساختار و سازگاری

- «بدون تغییر در اصل ساختار»: همه‌ی تغییرها روی همان الگوی موجود پروژه (کالکشن
  `src/lib/marketplace/*`، روترهای `src/app/api/marketplace/*` و کامپوننت‌های `Marketplace*`) نشسته‌اند و
  هیچ‌جای دیگر سایت تغییر نکرده است.
- سازگاری به‌عقب: اگر درخواست آپلودی `formatId` نداشته باشد (مثل اسکریپت‌های قدیمی)، از نام فایل حدس زده
  می‌شود؛ سفارش‌ها و لایسنس‌های قبلی دست‌نخورده کار می‌کنند.

## ۷) آنچه هنوز نیست (صادقانه)

- **افزودن رنگ تازه بعد از آپلود** فقط از راه API انجام می‌شود؛ در استودیوی هنرمند دکمه‌ی
  «افزودن رنگ» نداریم و متن راهنمای فرم هم همین را می‌گوید. اگر بخواهید، گام بعدی کوچکی است.
- فایل‌های وکتور (SVG/EPS/AI) فقط چک امضا و حجم می‌شوند، مثل قبل پیش‌نمایش تصویری از آن‌ها ساخته نمی‌شود
  (به همین دلیل اولین فایل هر رنگ باید PNG یا JPG باشد).
- «ویدیوهای این دوره» در صفحه‌ی دوره همچنان خالی است چون داده‌ی seed ویدیو ندارد — از کارهای قبلی باز مانده.
