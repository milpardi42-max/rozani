# گزارش کار — صفحه‌ی پروفایل هنرمند/طراح = فقط داشبورد هنرمند

**شاخه:** `arena/01a0cf92-rozvelt` · **PR:** [#1](https://github.com/milpardi42-max/Rozvelt/pull/1)

## خواسته‌ی شما

> «می‌خواهم ساختار صفحه‌ی پروفایل هنرمند/طراح را تغییر بدهم … وقتی من به‌عنوان هنرمند/طراح لاگین می‌کنم و
> وارد صفحه‌ی پروفایل می‌شوم، می‌خواهم محتوای این صفحه را حذف کنی و فقط محتوای تب «داشبورد هنرمند» را
> به‌عنوان صفحه‌ی اختصاصی برای این قسمت قرار بدهی — بدون این‌که به ساختار فرم لاگین دست بزنی.»

طبق تأیید شما: منظور صفحه‌ی **«حساب من»** یعنی مسیر `/{locale}/account` است و تغییر **فقط برای
هنرمند/طراح** اعمال می‌شود (خریدارها صفحه‌ی حساب خودشان را دست‌نخورده دارند).

## چه چیزی عوض شد

### ۱) `/{locale}/account` برای هنرمند/طراح = خودِ داشبورد هنرمند

- `src/app/[locale]/account/page.tsx` حالا یک کامپوننت **سرور** است: نشست را می‌خواند و اگر کاربر
  هنرمند/طراح باشد (نقش `artist` یا داشتن پرونده‌ی هنرمند)، به‌جای `AccountView` همان **داشبورد هنرمند** را
  رندر می‌کند.
- برای این کار یک پنل مشترک ساخته شد: `src/components/artist/ArtistDashboardPanel.tsx` — همان
  برِدکرامب + `ArtistDashboardView` که صفحه‌ی `/{locale}/artist` هم از آن استفاده می‌کند. یعنی دو مسیر
  **عیناً یک محتوا** دارند (کارت هویت و وضعیت تأیید، نوار راه‌اندازی استودیو، شش شاخص کلیدی، وضعیت آثار،
  کیف پول و تسویه، سرانه‌ی تحویل، پیشنهادهای فروش، آثار من، آخرین تراکنش‌ها و ترکیب فروش).
- **هیچ‌کدام از بخش‌های حساب خریدار روی این صفحه نمی‌ماند:** نه بنر رنگی و آواتار، نه منوی کناری
  (خلاصه حساب · رزروهای من · سفارش‌ها · تنظیمات)، نه کارت‌های علاقه‌مندی/سبد/سفارش‌ها.
- عنوان تب مرورگر هم برای هنرمند «داشبورد هنرمند» شد و برای خریدار همان «حساب کاربری» ماند.

### ۲) خریدار دست‌نخورده

کاربر خریدار (بدون پرونده‌ی هنرمند) همان `AccountView` قبلی را می‌بیند؛ هیچ خطی از آن عوض نشد.

### ۳) فرم لاگین دست‌نخورده

طبق خواسته‌ی شما هیچ تغییری در `AuthForm`، `LoginShell`، مسیر `/{locale}/login` یا ساختار فرم ورود داده
نشد. (فقط این‌که بعد از ورودِ هنرمند، همان صفحه‌ی مقصد یعنی `/account` حالا محتوای داشبورد را نشان می‌دهد.)

## تأیید زنده (روی بیلد پروداکشن)

- ریگ `scripts/marketplace-smoke/artist-dashboard-e2e.sh` بازنویسی/گسترش یافت و روی سرور تازه اجرا شد:
  **۸۲ بررسی، همه سبز، بدون skip و بدون هشدار** — از جمله سه بررسی تازه برای هر دو زبان:
  - «صفحه‌ی `/account` هنرمند، داشبورد هنرمند را نشان می‌دهد» (کارت‌ها و بخش‌های داشبورد در HTML سرور).
  - «و دقیقاً همان است — محتوایش با `/{locale}/artist` برابر است» (مقایسه‌ی متن دو صفحه، بایت‌به‌بایت).
  - «هیچ‌کدام از بخش‌های حساب خریدار روی آن نمانده است» (خلاصه حساب، رزروهای من، تنظیمات).
  - و در بخش خریدار: «صفحه‌ی حساب خریدار با بخش‌های خودش سر جایش است» (خلاصه حساب/رزروهای من در باندل
    کلاینت، و نبود نشانه‌های داشبورد هنرمند).
- بررسی زنده‌ی دستی: با ورود `niloufar@example.com`، متن صفحه‌ی `/fa/account` و `/fa/artist` **کاملاً یکسان**
  است (fa و en) و `/fa/account` · `/en/account` · `/fa/account/licenses` · `/fa/favorites` همه ۲۰۰ می‌دهند.
- بقیه‌ی ریگ‌ها روی همین بیلد سبز ماندند: `http` · `exclusive` · `artist` · `sub` · `family` (۳۳) ·
  `formats` (۶۲) · `multipart` (۳) · `pages.sh` · آکادمی. `tsc` و `eslint` پاک.

## چطور در پیش‌نمایش امتحان کنید

1. با حساب هنرمند خودتان وارد شوید و **«حساب من»** را باز کنید (`/fa/account`): فقط داشبورد هنرمند را
   می‌بینید — همان چیزی که در `/fa/artist` هست.
2. برای مقایسه، با یک حساب خریدار وارد شوید: همان صفحه‌ی حساب قبلی (خلاصه حساب، رزروها، سفارش‌ها،
   تنظیمات) دست‌نخورده است.
