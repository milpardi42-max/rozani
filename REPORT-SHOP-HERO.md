# گزارش کار — هیرو سکشن تازه‌ی صفحه‌ی فروشگاه

**شاخه:** `arena/01a0cf92-rozvelt` · **PR:** [#1](https://github.com/milpardi42-max/Rozvelt/pull/1)

خواسته‌ی شما: «برای صفحه‌ی فروشگاه، فقط در هیرو سکشن این صفحه یک هیرو سکشن جذاب و مرتبط قرار بده، با یک
ساختار متفاوت و حرفه‌ای.» — دقیقاً همین شد: **فقط هیرو** عوض شده؛ کاتالوگ زیر آن (بخش‌های خانواده، درخت
کناری، فیلترها، مرتب‌سازی) یک خط هم تغییر نکرده.

## ساختار قبلی هیرو
یک کارت دوستونه: سمت راست متن (برچسب، تیتر کالکشن، توضیح، دو دکمه‌ی ساده، یادداشت ارسال) و سمت چپ یک
تصویر تخت از یک محصول با نوار پایین (نشان «منتخب»، نام، SKU).

## ساختار تازه — یک سکشن، چهار حرکت

1. **پنل بوتیک (تیره و ادیتوریال):** پس‌زمینه‌ی تیره با هاله‌ی رنگی ملایم و خط تزئینی؛ برچسب
   «فروشگاه رزی آتلیه»؛ تیتر کالکشن؛ یک خط توضیح؛ دو دکمه‌ی واقعی (`Button`) برای دو در ورود به
   کاتالوگ: «اختصاصی رزی آتلیه» ← `?owner=site` و «اثر هنرمند» ← `?owner=artist`.
2. **اعداد زنده‌ی کاتالوگ** (داخل همان پنل، زیر خط جداکننده): تعداد محصول · خانواده‌های دارای محصول
   («۴/۸») · کل رنگ‌بندی‌ها · طراحان همکار — همه از داده‌ی واقعی فروشگاه محاسبه می‌شوند، نه دستی.
3. **موزاییک محصولات (سه کاشی، قد‌های نامساوی):**
   - **قطعه‌ی شاخص:** بزرگ‌ترین کاشی، تصویر محصول با زوم نرم روی هاور، نشان «منتخب» + نشان «جدید» (اگر
     تازه باشد)، فلش ظریف، و نوار پایین با **خانواده · سازنده · نام · SKU · قیمت**.
   - **قطعه‌ی دوم:** محصول دیگری از آتلیه با خانواده و قیمت.
   - **کاشی رنگ‌بندی‌ها:** چهار رنگ‌بندیِ قطعه‌ی شاخص به‌صورت تصویر + نقطه‌ی رنگ + نام، با شمارش کل و
     لینک «همه‌ی رنگ‌بندی‌ها».
4. **ریل پایانی داخل همان هیرو:** هر هشت خانواده‌ی سطح به‌شکل چیپ با تعداد واقعی‌شان، هرکدام لینک
   `?family=<slug>` (یعنی از همان هیرو می‌توانید کاتالوگ را دسته‌به‌دسته مرور کنید) + یادداشت سرویس
   (بنر ارسال رایگان) در سمت دیگر.

نتیجه: هیرو هم **مرتبط** است (همه‌چیزش از خودِ کاتالوگ می‌آید: محصول واقعی، قیمت واقعی، رنگ‌بندی واقعی،
شمارش واقعی) و هم ساختارش با قبل **کاملاً متفاوت** است (پنل تیره + عدد‌های زنده + موزاییک + ریل خانواده).

## کد
- فایل تازه: `src/components/shop/ShopHero.tsx` (کامپوننت **سرور**؛ پس اعداد و محصولات در همان HTML اول
  هستند و SEO/سرعت دست‌نخورده می‌ماند).
- `src/app/[locale]/shop/page.tsx`: بلوک هیروی قبلی جای خود را به `<ShopHero …>` داد و محصولات
  (`enrichProduct`) برای انتخاب کاشی‌ها آماده شدند. هیچ تغییر دیگری در این فایل نیست.
- عنوان تب مرورگر، متادیتا، بردکرامب، متن‌ها و مسیرها مثل قبل؛ تیتر کاتالوگ («فروشگاه سطح و دکور» / نتیجه‌ها)
  دست‌نخورده.

## تأیید زنده
- ریگ تازه‌ی `scripts/marketplace-smoke/shop-hero-e2e.sh`: **۷۴ بررسی، همه سبز** (`/tmp/rig-shop2.log`) —
  برای هر دو زبان: پنل و دو دکمه‌ی ورودی، **بازخوانی چهار عدد از خودِ استور و مقایسه با هیرو**، موزاییک
  (لینک قطعه‌ی شاخص · نشان · SKU · خانواده · قطعه‌ی دوم · نام و تصویر رنگ‌بندی‌ها)، هشت چیپ خانواده با
  تعداد واقعی، یادداشت سرویس، و سالم بودن کاتالوگ زیر هیرو (بخش‌های خانواده، شمارنده‌ی نتیجه، جداسازی
  `?family=`).
- ریگ تکسونومی (`family-e2e.sh`) هم دوباره سبز شد: **۳۳ ✔** — یعنی فیلتر خانواده، درخت کناری و ترتیب
  بخش‌ها را نشکسته‌ام. `pages.sh` حالا `/fa|en/shop` و `?family=wallpaper` را هم می‌سنجد (۲۰۰).
  `portfolio-e2e.sh` هم دوباره **۷۸ ✔ / ۰ ✘**. `tsc --noEmit` و `eslint` پاک.
- بررسی زنده‌ی متن صفحه: در fa هیرو این‌ها را می‌دهد: «فروشگاه رزی آتلیه · کالکشن اختصاصی آتلیه · …
 ۱۰ محصول · ۴/۸ خانواده‌ی سطح · ۳۵ رنگ‌بندی‌ها · ۴ طراح همکار · پرده — باغ آرام · RA-CR-2005 ·
  ‎۴,۲۰۰,۰۰۰ تومان · ست دکور — کوسن‌های الگو · عاجی/سبز مریم/شفاف · مرور بر اساس خانواده + هشت چیپ ·
 ارسال رایگان» و در en معادل‌هایشان.

## چطور ببینید
`fa/shop` و `en/shop` را باز کنید (پیش‌نمایش روی همین بیلد بالاست). اگر خواستید قطعه‌ی شاخص موزاییک
محصول دیگری باشد یا کاشی سومی اضافه شود، بگویید.
